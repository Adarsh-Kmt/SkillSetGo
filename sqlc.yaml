version: "1"
packages:
  - name: "db"                 # Name of the package generated by sqlc
    path: "./db" # Directory to place the generated code
    queries: "./queries.sql"   # Path to the SQL queries file
    engine: "postgresql"       # SQL dialect (PostgreSQL in this case)
    schema: "./schema.sql"